<%= form_for @record do |f| %>
  <div>
    <%= f.label :artist %>
    <%= f.text_field :artist %>
  </div>

  <div>
    <%= f.label :title %>
    <%= f.text_field :title %>
  </div>

  <div>
    <%= f.label :cover %>
    <%= f.file_field :cover %>
  </div>

  <%= f.submit "submit"%>

<% end %>