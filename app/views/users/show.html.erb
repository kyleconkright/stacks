<div id="profile-container">
	<section id="my-stack" class="record-list">
		
		<% if current_user == @user  %>
			<h2>My Stack</h2>
		<% else %>
			<h2><%= @user.first_name %>'s Stack</h2>
		<% end %>

		
		<% unless @user_stack.count == 0 %>
			<ul>
			<% @user_stack.each do |record| %>
			    <li>
			        <div class="cover"><%= link_to image_tag(record.cover_url, size: '300x300'), record_path(record) if record.cover?  %></div>
			        <div class="info">
			            <div class="meta">    
			                <p class="artist"><%= link_to record.artist, record_path(record) %></p>
			                <p class="title"><%= record.title %></p>
			            </div>
			        </div><!--info -->
			    </li>
			<% end %>
			</ul><!-- full-list -->
		<% else %>
			<ul><li>Nothing here... <%= link_to "Browse", records_path %> or <%= link_to "Add a new Record", new_record_path %></li></ul>
		<% end %>
	</section><!-- my_stack -->


	<section id="my-list" class="record-list">
		<% if current_user == @user  %>
			<h2>My Wantlist</h2>
		<% else %>
			<h2><%= @user.first_name %>'s Wantlist</h2>
		<% end %>
		<% unless @user_wants.count == 0 %>
			<ul>
			<% @user_wants.each do |record| %>
			    <li>
			        <div class="cover"><%= link_to image_tag(record.cover_url, size: '300x300'), record_path(record) if record.cover?  %></div>
			        <div class="info">
			            <div class="meta">    
			                <p class="artist"><%= link_to record.artist, record_path(record) %></p>
			                <p class="title"><%= record.title %></p>
			            </div>
			        </div><!--info -->
			    </li>
			<% end %>
			</ul><!-- full-list -->
		<% else %>
			<ul><li>Nothing here... <%= link_to "Browse", records_path %> or <%= link_to "Add a new Record", new_record_path %></li></ul>
		<% end %>
	</section><!-- my_wants -->

</div><!-- profile-container -->